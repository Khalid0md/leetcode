def BackTrack(res, args):
    if GOAL REACHED:
        add solution to res
        return
    for choice in choices:
        if (CHOICE IS VALID)
            make choice
            backtrack(res, args)
            undo choice